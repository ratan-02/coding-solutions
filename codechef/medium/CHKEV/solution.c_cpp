#include <bits/stdc++.h>
using namespace std;

int main() {
    int x, y;
    cin >> x >> y;

    if (x % 2 == 0 || x < y)
        cout << "Yes";
    else
        cout << "No";

    return 0;
    
    
}